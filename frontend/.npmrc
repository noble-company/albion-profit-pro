# openapi-typescript@7.13.0 declara peer de typescript@^5, mas o projeto usa TypeScript 6
# (decisão da Fase 3, achado W6 — gerador funciona, typecheck/build passam). Sem isto, todo
# `npm install` que adiciona pacote falha no ERESOLVE.
legacy-peer-deps=true
save-exact=true
